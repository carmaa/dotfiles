1. Install Xcode Command Line Tools by running `xcode-select --install`.
2. Create a SSH key and copy/paste to Github
3. Clone repo: `git clone git@github.com:carmaa/dotfiles.git`
4. Cnstall homebrew: `cd dotfiles` and `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`
5. Install apps: `brew bundle`
6. Install config: 
